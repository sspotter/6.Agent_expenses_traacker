Good catch — this is **not a random browser issue**, it’s a **react → supabase request loop** that just happens to surface as
`net::ERR_INSUFFICIENT_RESOURCES`.

Let’s **extend the task list properly** and explicitly include this bug, because it’s tightly related to the Telegram + Worker save flow.

---

## 🔁 Worker Save Loop & Supabase Request Storm – Tasks (Added)

### 10️⃣ Identify the loop trigger (root cause)

* [ ] Reproduce the issue by clicking **Save Changes** in `WorkerModal`
* [ ] Identify which state update causes:

  * `settlements?select=amount&applied_to_month=...`
  * `daily_collections?...`
    to refetch repeatedly
* [ ] Confirm whether the loop is caused by:

  * `useEffect` dependency changes
  * `useMemo` with unstable dependencies
  * refetch inside render / derived state

⚠️ This error:

```
net::ERR_INSUFFICIENT_RESOURCES
```

means **the browser is choking on too many concurrent requests**, not a Supabase outage.

---

### 11️⃣ Fix `useEffect` dependency explosion

* [ ] Audit all `useEffect` hooks triggered after worker save
* [ ] Remove objects / arrays from dependency lists
* [ ] Replace unstable deps with primitives (ids, strings)
* [ ] Ensure **save mutation does NOT trigger refetch loop**

Common anti-pattern to fix:

```ts
useEffect(() => {
  fetchMonthlySummary();
}, [worker]); // ❌ worker object changes every render
```

Correct pattern:

```ts
useEffect(() => {
  fetchMonthlySummary();
}, [worker.id, currentMonth]); // ✅ stable
```

---

### 12️⃣ Separate “save” side effects from “read” queries

* [ ] Ensure saving worker data does **not**:

  * update global state that triggers summary recalculation
  * re-run monthly settlement aggregation
* [ ] Decouple:

  * `onSaveWorker`
  * `fetchMonthlyData`
* [ ] Use explicit refetch calls instead of implicit state coupling

---

### 13️⃣ Guard Supabase queries with conditions

* [ ] Add early returns in hooks:

  ```ts
  if (!workerId || !month) return;
  ```
* [ ] Prevent queries from running during:

  * modal open
  * save in progress
* [ ] Ensure no query runs on **every render**

---

### 14️⃣ Deduplicate monthly fetches

* [ ] Ensure settlements query runs **once per month**
* [ ] Ensure daily collections query runs **once per month**
* [ ] Cache results with `useMemo` or local state
* [ ] Do not refetch on unrelated UI changes (Telegram section edits)

---

### 15️⃣ Fix WorkerModal save flow

* [ ] Disable Save button while saving
* [ ] Prevent double-submit
* [ ] Ensure modal close does not retrigger data fetch
* [ ] Ensure Telegram chat CRUD does not touch settlement state

---

### 16️⃣ Add request throttling protection (defensive)

* [ ] Add basic debounce to summary recalculation
* [ ] Prevent overlapping Supabase calls for same query
* [ ] Optional: add simple in-flight flag

  ```ts
  if (isFetching) return;
  ```

---

### 17️⃣ Verification checklist (mandatory)

* [ ] Save worker → **no request flood**
* [ ] Network tab shows:

  * 1 settlements request
  * 1 daily_collections request
* [ ] CPU + memory stable
* [ ] Monthly summary still correct
* [ ] Telegram messages still send to all chat IDs

---

### 18️⃣ Cleanup & documentation

* [ ] Add comment explaining why dependencies are constrained
* [ ] Remove any leftover debugging logs
* [ ] Mark task as fixed in `task.md`

---

## Updated Definition of Done (DoD)

✅ Telegram summary sends to **all configured chat IDs**
✅ Worker save does **not** trigger refetch loops
✅ No `ERR_INSUFFICIENT_RESOURCES` in console
✅ Monthly queries are stable, bounded, and intentional

---
